<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapper.BackOfficeMapper">

	<select id="selectBoInquiryList"  resultType="com.vo.Inquiry" >
		SELECT 
			id, 
			title, 
			context, 
			regDt, 
			regId, 
			updateDt, 
			updateId, 
			imgGroupId 
		FROM 
			inquiry 
	</select>
	
	<select id="selectBoMemberList"  resultType="com.vo.Member">
		SELECT 
			id,
			nm, 
			userId, 
			birth, 
			pno, 
			addr1, 
			addr2, 
			regDt, 
			regId, 
			updateDt, 
			updateId 
		FROM 
			member
	</select>

</mapper>


